# S3-Compatible Storage Configuration
# This service works with AWS S3 and S3-compatible storage providers like Contabo, MinIO, etc.

# AWS Region (e.g., us-east-1, eu-west-1, ap-southeast-1)
AWS_REGION=us-east-1

# AWS Credentials (required unless using IAM roles)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# S3 Bucket name (required)
AWS_S3_BUCKET=your-bucket-name

# Custom endpoint for S3-compatible services (leave empty for AWS S3)
# Examples:
# Contabo: https://usc1.contabostorage.com
# MinIO: http://localhost:9000
# DigitalOcean Spaces: https://nyc3.digitaloceanspaces.com
AWS_S3_ENDPOINT=

# Force path-style addressing (required for some S3-compatible services)
# Set to 'true' for Contabo, MinIO, etc. Leave empty or 'false' for AWS S3
AWS_S3_FORCE_PATH_STYLE=false

# Contabo-specific: Public URL identifier (only needed for Contabo Object Storage)
# Find this in your Contabo member panel - it's the ID before the colon in public URLs
# Example: if your public URL is https://usc1.contabostorage.com/abc123:bucket/file
# then set CONTABO_PUBLIC_URL_ID=abc123
CONTABO_PUBLIC_URL_ID=

# Optional: File upload limits (in bytes)
# MAX_FILE_SIZE=10485760  # 10MB
# MAX_FILES_PER_REQUEST=10
